import { normalizeFilms } from "./normalize-films";
import { normalizePeople } from "./normalize-people";
import { normalizePlanets } from "./normalize-planets";
import { normalizeSpecies } from "./normalize-species";
import { normalizeStarships } from "./normalize-starships";
import { normalizeVehicles } from "./normalize-vehicles";

normalizeFilms();
normalizePeople();
normalizePlanets();
normalizeSpecies();
normalizeStarships();
normalizeVehicles();
